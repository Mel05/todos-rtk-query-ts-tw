export const getRequestPath = () => {
	const requestPath = `https://todos-server.up.railway.app/`
	// const requestPath = `https://todos-server-supermadmel.onrender.com`
	// const requestPath = `http://localhost:3011`

	return {
		requestPath,
	}
}
